{% block content %}
<nav class=" backdrop-blur shadow">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
            <a href="/" class="flex-shrink-0 flex items-center">
                <img src="{{ asset('build/images/logo.jpg') }}" width="120" height="auto" class="h-12 w-auto" alt="Brand">
            </a>
            <!-- Burger button for mobile -->
            <button id="navbar-toggle" type="button" class="md:hidden inline-flex items-center justify-center p-2 rounded-md text-red-600 hover:bg-red-100 focus:outline-none">
                <svg class="h-6 w-6" stroke="currentColor" fill="none" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                </svg>
            </button>
            <!-- Menu -->
            <ul id="navbar-menu" class="hidden md:flex md:items-center md:space-x-6 text-center absolute md:static left-0 right-0 md:bg-transparent bg-white md:shadow-none shadow-lg md:mt-0 mt-2 py-2 md:py-0 z-50">
                
                <li>
                    <a href="{{ path('app_about') }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">A propos</a>
                </li>
                <li>
                    <a href="{{ path('instrument_synths') }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">Synthétiseurs</a>
                </li>
				
                {% if is_granted('ROLE_USER') %}
				 <li>
                    <a href="{{ path('app_instrument_search') }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">Chercher un instrument</a>
                </li>
                <li>
                    <a href="{{ path('app_instrument_actu') }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">Actualités</a>
                </li>
				<li>
                    <a href="{{ path('app_home_connected') }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">Home</a>
                </li>
                    <li>
                        <a href="{{ path('app_logout') }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">Déconnexion</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('app_login') }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">Connexion</a>
                    </li>
                    <li>
                        <a href="{{ path('app_register') }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">Inscription</a>
                    </li>
                {% endif %}
                {# {% if is_granted('ROLE_USER') %}
                    <li>
                        <a href="{{ path('app_dresseur_show', {'id': app.user.id}) }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">Mon profil</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li>
                            <a href="{{ path('app_youtube_search', {'query': 'terme de recherche'}) }}" class="block px-4 py-2 text-lg text-red-600 hover:text-red-800 font-semibold">Rechercher sur YouTube</a>
                        </li>
                    {% endif %} #}
                {# {% endif %} #}
            </ul>
        </div>
    </div>
</nav>
{% endblock %}

<script>
    // JS natif pour le menu mobile
    const toggle = document.getElementById('navbar-toggle');
    const menu = document.getElementById('navbar-menu');
    toggle.addEventListener('click', () => {
        menu.classList.toggle('hidden');
    });
</script>

